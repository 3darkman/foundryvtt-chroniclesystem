{{!-- Fallback Sistema body (spec 019). The PARTS default before
      _configureRenderParts swaps in `system/<type>.hbs` per document type. --}}
<section class="tab {{#if tab.active}}active{{/if}}" data-group="primary" data-tab="system"></section>
